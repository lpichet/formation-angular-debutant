import { PricePartsPipe } from './price-parts.pipe';

describe('PricePartsPipe', () => {
  it('create an instance', () => {
    const pipe = new PricePartsPipe();
    expect(pipe).toBeTruthy();
  });
});
